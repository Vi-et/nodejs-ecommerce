services:
  redis:
    image: redis:alpine
    container_name: nodejs-ecommerce-redis
    ports:
      - "${REDIS_PORT:-6379}:6379"
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    networks:
      - kafka-network

  kafka:
    image: bitnamilegacy/kafka:latest
    container_name: kafkaMQ
    ports:
      - "9092:9092"
      - "9094:9094"
    environment:
      - KAFKA_CFG_NODE_ID=1
      - KAFKA_CFG_PROCESS_ROLES=controller,broker
      - KAFKA_CFG_LISTENERS=PLAINTEXT://:9092,CONTROLLER://:9093,EXTERNAL://:9094
      - KAFKA_CFG_ADVERTISED_LISTENERS=PLAINTEXT://kafkaMQ:9092,EXTERNAL://localhost:9094
      - KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP=CONTROLLER:PLAINTEXT,EXTERNAL:PLAINTEXT,PLAINTEXT:PLAINTEXT
      - KAFKA_CFG_CONTROLLER_QUORUM_VOTERS=1@localhost:9093
      - KAFKA_CFG_CONTROLLER_LISTENER_NAMES=CONTROLLER
      - KAFKA_CFG_INTER_BROKER_LISTENER_NAME=PLAINTEXT
    networks:
      - kafka-network
    volumes:
      - kafka_data:/bitnami/kafka
  rabbitmq:
    image: rabbitmq:3-management # Bắt buộc phải có chữ "management"
    container_name: rabbitmq-server
    ports:
      - "5672:5672" # Cửa phụ để code Node.js gửi/nhận tin nhắn
      - "15672:15672" # Cửa chính để bạn truy cập giao diện web

volumes:
  redis_data:
  kafka_data:

networks:
  kafka-network:
    driver: bridge
